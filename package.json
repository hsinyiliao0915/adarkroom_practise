{
  "name": "adarkroom-phaser",
  "description": "A Dark Room recreated in Phaser 3 & TypeScript (For practice and learning purposes only / 僅供個人學習與技術練習使用)",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "test": "tsx --test tests/**/*.test.ts"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.12.0",
    "tsx": "^4.23.13",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
