{
  "name": "adarkroom-phaser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "@fission-ai/openspec": "^1.12.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
